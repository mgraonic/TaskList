<h2>Task List</h2>


<ul class="task-list">
  <% @tasks.each do |task| %>
  <li>
    <!-- <input id="checkBox" type="checkbox" action="/tasks/edit" method="patch"><%link_to(task_path(task), method: :update, data: {confirm: "gonna mark this complete?"}) %></br> -->
    <%= task.name %>
    <%= link_to("(details)", task_path(task)) %>
    <%= link_to("(edit)", edit_task_path(task))%>
    <%= link_to("(delete)", task_path(task), method: :delete, data: {confirm: "Are you sure you want to delete this task?"})%>

    <!-- <%= button_to('Update Action', task_path(task), method: :patch) %> -->
    <!-- <%= link_to "Complete", task_path(task.id), method: :put %> -->
    <!-- <%= link_to("Mark Complete", complete_task_path(task), method: :patch) %> -->

  </li>
  <%end%>
</ul>
